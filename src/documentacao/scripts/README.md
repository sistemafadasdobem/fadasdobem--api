# Documentação dos *scripts* (`scripts/`)

Ferramentas de operação e dados fora do ciclo request/response. Caminho no repositório: **`fadasdobem--api/scripts/`** (raiz do pacote API, não sob `src/`).

| Documento | Conteúdo |
|-----------|----------|
| [Registo_Scripts.md](./Registo_Scripts.md) | `run-migrations`, `seed-dev-homolog`, SQL auxiliar |

**package.json:** procure `migrate`, `seed:homolog` e semelhantes para comandos exactos.
